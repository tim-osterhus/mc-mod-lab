diff --git a/packages/common/src/main/java/xyz/langyo/minecraft/mcp/common/ReflectedInputHandler.java b/packages/common/src/main/java/xyz/langyo/minecraft/mcp/common/ReflectedInputHandler.java
--- a/packages/common/src/main/java/xyz/langyo/minecraft/mcp/common/ReflectedInputHandler.java
+++ b/packages/common/src/main/java/xyz/langyo/minecraft/mcp/common/ReflectedInputHandler.java
@@ -328,6 +328,13 @@ public class ReflectedInputHandler extends McpMessageHandler implements McpProtocol.MinecraftInput {
             Object mc = mc();
             int w = getWidth();
             int h = getHeight();
+            // Read the active framebuffer size, not stale logical window fields.
+            java.nio.IntBuffer viewport = java.nio.ByteBuffer.allocateDirect(16)
+                    .order(java.nio.ByteOrder.nativeOrder()).asIntBuffer();
+            Class.forName("org.lwjgl.opengl.GL11").getMethod("glGetIntegerv", int.class,
+                    java.nio.IntBuffer.class).invoke(null, 0x0BA2, viewport);
+            w = viewport.get(2);
+            h = viewport.get(3);
             if (w <= 0 || h <= 0) {
                 java.lang.reflect.Field winField = ReflectionCache.getDiscoveredField("window");
                 if (winField != null) {
diff --git a/packages/common/src/main/java/xyz/langyo/minecraft/mcp/common/ScreenshotHelper.java b/packages/common/src/main/java/xyz/langyo/minecraft/mcp/common/ScreenshotHelper.java
--- a/packages/common/src/main/java/xyz/langyo/minecraft/mcp/common/ScreenshotHelper.java
+++ b/packages/common/src/main/java/xyz/langyo/minecraft/mcp/common/ScreenshotHelper.java
@@ -43,7 +43,7 @@ public final class ScreenshotHelper {
 
     private static byte[] takeScreenshot0(Object mc, int width, int height) {
         byte[] cached = cachedScreenshot;
-        if (cached != null && System.currentTimeMillis() - cachedScreenshotTime < 2000) {
+        if (videoCaptureActive && cached != null && System.currentTimeMillis() - cachedScreenshotTime < 2000) {
             ReflectionHelper.dbg("takeScreenshot: returning cached screenshot " + cached.length + " bytes (age " + (System.currentTimeMillis() - cachedScreenshotTime) + "ms)");
             return cached;
         }
@@ -349,7 +349,7 @@ public final class ScreenshotHelper {
                         try { int v = f.getInt(fb); if (v > 0) { if (fbw == 0) fbw = v; else if (fbh == 0) fbh = v; } } catch (Exception ignored) {}
                     }
                 }
-                if (fbw > 0 && fbh > 0) { width = fbw; height = fbh; }
+                // Keep the caller's measured viewport; field order is not a size contract.
             } catch (Exception ignored) {}
         }
         int w = width, h = height;
@@ -749,6 +749,7 @@ public final class ScreenshotHelper {
     }
 
     public static void cacheFrameFromRenderThread(Object mc) {
+        if (!videoCaptureActive) return;
         if (System.currentTimeMillis() - cachedScreenshotTime < 1000) return;
         try {
             Object rt = getMainRenderTarget(mc);
